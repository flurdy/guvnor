extend ../layout
block content

   #breadcrumb project/#{project.title}/versions

   h4 project #{project.title} environment versions

   .well
      table.table.table-striped
        each environment in environments.funnel
           tr
               td.pull-right #{environment.environment}
               td #{environment.version}
